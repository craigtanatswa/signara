/** Uniform downscale applied when capturing signatures (draw / type / upload). */
export const SIGNATURE_DISPLAY_SCALE = 0.7

/** Pad / wizard preview thumbnail. */
export const SIGNATURE_PREVIEW_CLASS =
  'max-h-[5.6rem] w-auto object-contain'

/** Saved-tab list thumbnail (smaller). */
export const SIGNATURE_SAVED_THUMB_CLASS =
  'max-h-11 w-auto object-contain'

/** Signature rendered on a document field. */
export const SIGNATURE_DOCUMENT_CLASS =
  'max-h-14 max-w-[154px] object-contain'

/** Review / detail page signature with border padding. */
export const SIGNATURE_REVIEW_CLASS =
  'max-h-14 w-auto object-contain rounded border border-signara-steel/20 bg-white p-1'

/** Document detail initiator signature block. */
export const SIGNATURE_DETAIL_CLASS =
  'mt-2 max-h-[4.2rem] w-auto object-contain'
